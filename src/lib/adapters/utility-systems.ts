export function getUtilitySystemStatuses() {
  return [
    {
      name: "Utility Systems",
      mode: "mock" as const,
      detail: "OMS, GIS, SCADA, AMI, WFM, CRM, parts, weather, and traffic are seeded fixtures.",
    },
  ];
}
